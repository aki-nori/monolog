<div class="row my-3">
  <div class="col-md-12 col-lg-3">
    <!-- md以上 -->
    <div class="d-md-block sticky">
      <%= render 'users/info', user: @user %>
    </div>
    <!-- sm以下 -->
    <div class="d-none ">
      <%= render 'users/info', user: @user %>
    </div>
  </div>
  <div class="col-md-12 col-lg-9">
  	<div class="alert alert-dismissible alert-success sticky z-index-200">
  	<strong><%= "#{@user.name}さんは#{@user.followings.count}人にフォローされています" %></strong>
	</div>
      <div class="row z-index-100">
        <% @user.followings.each do |user| %>
        <div class="col-lg-4 col-md-6">
        <%= render 'users/user_card', user: user %>
        </div>
        <% end %>
      </div>
  </div>
</div>